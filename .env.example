# Frontend configuration. Copy to .env.local for local development, or set the
# same variable in the Vercel dashboard for the deployed site.
#
# Where the CallPal API lives. Leave unset locally — it defaults to
# http://127.0.0.1:8000.
VITE_API_URL=
